
### ✅ 已完成的功能

1. **敏感信息安全存储**
   - 将API密钥和token移动到 `~/.config/napcat-backend/secrets.env`
   - 更新 `.gitignore` 防止敏感信息上传到GitHub
   - 项目根目录的 `.env` 文件已清空敏感信息

2. **自动启动脚本**
   - `start-napcat.sh` - 完整的启动/停止/重启脚本
   - `test-start.sh` - 简化的测试脚本
   - `install-service.sh` - 系统服务安装脚本

3. **系统服务配置**
   - 创建systemd服务文件 `napcat-backend.service`
   - 支持开机自动启动
   - 支持服务状态监控和日志管理

4. **完整的文档**
   - 详细的使用说明和故障排除指南
   - 常用命令参考
   - 安全说明

## 🚀 快速使用指南

### 1. 立即启动服务
```bash
# 测试并启动服务
~/.config/napcat-backend/test-start.sh

# 然后启动服务
~/.config/napcat-backend/start-napcat.sh start
```

### 2. 设置开机自动启动
```bash
# 安装系统服务（只需运行一次）
~/.config/napcat-backend/install-service.sh
```

### 3. 常用管理命令
```bash
# 查看服务状态
~/.config/napcat-backend/start-napcat.sh status

# 停止服务
~/.config/napcat-backend/start-napcat.sh stop

# 重启服务
~/.config/napcat-backend/start-napcat.sh restart

# 查看实时日志
~/.config/napcat-backend/start-napcat.sh logs
```

## 📁 文件结构

```
~/.config/napcat-backend/
├── secrets.env              # 敏感配置文件（API密钥、Token等）
├── start-napcat.sh          # 主要启动脚本
├── test-start.sh           # 测试脚本
├── install-service.sh      # 服务安装脚本
├── README.md               # 详细使用说明
└── napcat.log              # 运行日志（自动生成）

~/.config/systemd/user/
└── napcat-backend.service  # systemd服务配置
```

## 🛠️ 故障排除

如果服务启动失败，请检查：

1. **PostgreSQL是否运行**
   ```bash
   pg_isready -h localhost -p 5432
   ```

2. **配置文件是否存在**
   ```bash
   ls -la ~/.config/napcat-backend/secrets.env
   ```

3. **查看日志**
   ```bash
   tail -f ~/.config/napcat-backend/napcat.log
   ```

4. **重新编译项目**
   ```bash
   cargo build --release
   ```

## 📋 系统服务命令

```bash
# 查看服务状态
systemctl --user status napcat-backend

# 启动/停止/重启服务
systemctl --user start napcat-backend
systemctl --user stop napcat-backend
systemctl --user restart napcat-backend

# 查看系统日志
journalctl --user -u napcat-backend -f
```

## 🎉 恭喜！

您的NapCat Backend现在具备：
- ✅ 安全的敏感信息存储
- ✅ 一键启动/停止功能
- ✅ 开机自动启动
- ✅ 完整的日志管理
- ✅ 系统服务集成

现在您可以轻松地管理您的QQ机器人服务了！如果需要修改API密钥或其他配置，只需编辑 `~/.config/napcat-backend/secrets.env` 文件即可。